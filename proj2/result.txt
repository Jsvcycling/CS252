Joshua Vega


3 changes, Xasy to rXplacX


 ******************

C:\Users\Josh\Documents\GitHub\jsvcycling\cs252\proj2\replace.S
@ replace.S: routine to replace a character in a string with another character

         .global replace
         .text
replace: stmfd sp!, {v1-v8, lr}
         mov v1, #0
         mov v2, #0
loop:    ldrb v3, [a1, v1]
         cmp a2, v3
         streqb a3, [a1, v1]
         addeq v2, v2, #1
         add v1, v1, #1
         cmp v3, #0
         bne loop
         mov a1, v2
         ldmfd sp!, {v1-v8, pc}
         .end

------------------

C:\Users\Josh\Documents\GitHub\jsvcycling\cs252\proj2\cmain.c
#include <stdio.h>
#include <stdlib.h>

extern int replace(char *s, char lr, char rp);

int main(int argc, char *argv[]) {
    int result;
    char lr = 'e';
    char rp = 'X';
    char str[] = "easy to replace";

    result = replace(str, lr, rp);
    printf("%d changes, %s\n", result, str);

    exit(0);
}
